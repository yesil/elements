export const ZOOM = {
  MIN: 1.0,
  MAX: 4.0,
  STEP: 0.1,
};

export const PAN = {
  EDGE_SAFETY_PX: 8,
  TOP_CLEARANCE_PX: 24,
};

/**
 * Pointer/drag configuration.
 */
export const DRAG = {
  START_THRESHOLD_PX: 2,
  DELTA_IS_SCREEN_SPACE: true,
  ZOOM_GAIN_EXP: 0.8,
  MIN_GAIN: 1.0,
  DEBUG: false,
};

export const ZOOM_DEBUG = false;
